<apex:page controller="ATBCController" cache="true" expires="600">
<script type="text/javascript">
var t=setTimeout("alert('I am displayed after 3 seconds!')",30000000)

</script>
 <apex:form >
   <!-- 
<apex:pageBlock title="Lead">
     <apex:outputField value="{!Lead.Name}" />
     <apex:outputField value="{!Lead.Phone}" />

       <apex:selectList value="{!statusToSet}">
         <apex:selectOption itemValue="Verified" itemLabel="Verified" />
         <apex:selectOption itemValue="Not Verified" itemLabel="Not Verified" />
         <apex:selectOption itemValue="Unknown" itemLabel="Unknown" />
       </apex:selectList>
       <apex:commandButton action="{!updateStatus}" value="Update" />
    
   </apex:pageBlock>
    -->
    
      <apex:commandButton action="{!save}" value="Test ATBC"/>
    </apex:form>
</apex:page>